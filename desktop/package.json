{
  "name": "onlance-desktop",
  "version": "0.1.8",
  "private": true,
  "description": "Application PC d'On lance ? — coquille Tauri.",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build && node renommer-installateur.mjs",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
